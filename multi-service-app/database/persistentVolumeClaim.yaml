apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-pvc
  namespace: pets-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 1Gi
  volumeName: database-pv